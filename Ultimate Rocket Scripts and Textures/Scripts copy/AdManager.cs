using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using UnityEngine.Advertisements;

public class AdManager : MonoBehaviour
{
    /* string adid = "3507988";
    string videoad = "video";
    string playVideoad = "rewardedVideo";
    int AdChance;
    private void Start()
    {
        Advertisement.Initialize(adid, true);
    }

    public void WatchAd()
    {
        if (Advertisement.IsReady(playVideoad))
        {
            Advertisement.Show(playVideoad);
            PlayerPrefs.SetInt("DoubleCoin", 1);
        }
    }
    public void PossibleAd()
    {
        AdChance = Random.Range(0, 3);
        if(AdChance == 1 && Advertisement.IsReady(videoad))
        {
            Advertisement.Show(videoad);
        }
    } */
}
